{% extends 'base.html.twig' %}

{% block title %}Registration{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="flex">
        <button class="btn btn-lg btn-primary" type="submit">
            Register
        </button>
        <a href="{{ path('app_login') }}" class="btn btn-lg btn-secondary">Already have account? Log in</a>
    </div>
    {{ form_end(form) }}
{% endblock %}
